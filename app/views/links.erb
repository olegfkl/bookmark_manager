<ul id='links'>
  <% @links.each do |link| %>
  <li>
  Title: <%= link.title %>
    URL: <%= link.url %>
</li>
<% end %>
</ul>
<form action='/links/new'>
<button type="submit" value='Add new link'>Add new link</button>
</form>
